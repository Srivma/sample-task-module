doctype html
html(lang='en')
  head
    style.
      body {
        margin: 0;
      }

    title Microsoft Teams Task Module Sample App
    link(rel='stylesheet', type='text/css', href='/styles/msteams-16.css')
    link(rel='stylesheet', type='text/css', href='/styles/custom.css')

    script(src='/scripts/bundle.js') // bundled version of the above files
    script.
      let appId = "#{appId}";
    block scripts

  body(class='theme-light')

    div(class='surface')
      div(class='panel')
        block content